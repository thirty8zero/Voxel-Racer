using UnityEngine;

namespace VoxelRacer
{
    /// <summary>Place on each engine-owned exhaust tip; local +Z points out of the pipe.</summary>
    [DisallowMultipleComponent]
    public sealed class VoxelEngineExhaustOutlet : MonoBehaviour
    {
    }
}
